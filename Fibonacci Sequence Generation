// Given a natural number $n$, display the first $n$ terms of the Fibonacci sequence in increasing order.

#include <iostream>
using namespace std;
int main()
{
    int i,n,v[100];
    cin>>n;
    v[0]=v[1]=1;
    for(i=2;i<n;i++)
    {
        v[i]=v[i-1]+v[i-2];
    }
    for(i=0;i<n;i++)
        cout<<v[i]<<' ';
    return 0;
}
