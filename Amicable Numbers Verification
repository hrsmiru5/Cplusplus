// Given two natural numbers, determine if they are "friends" (amicable numbers). Amicable numbers are pairs of numbers where each number is equal to the sum of the proper divisors of the other (the sum of all divisors excluding the number itself).

#include <iostream>
using namespace std;
int suma(int n)
{
    int s=1,d;
    for(d=2;d<=n/2;d++)
    {
        if(n%d==0)
            s=s+d;
    }
    return s;
}
int main()
{
    int a,b;
    cin>>a>>b;
    if(suma(a)==b && suma(b)==a)
        cout<<"PRIETENE";
    else cout<<"NU SUNT PRIETENE";
    return 0;
}
